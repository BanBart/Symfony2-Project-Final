{% form_theme form 'ProjectLineOfWorkBundle:Form:fields.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="POST" class="fos_user_resetting_reset">
    {{ form_widget(form) }}
    <div>
        <button class='btn custom-button pull-right' type="submit">Confirm</button>
    </div>
</form>
